# Fixture inputs

The `themes/` directory contains small regular-file themes used to exercise
the `--theme` copy path. Templates and themes are explicit filesystem inputs;
the generator does not embed an external corpus or infer one from the
working directory.
