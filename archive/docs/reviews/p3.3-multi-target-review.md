# Review record — P3.3 Multi-Target Isolated Outputs

**Date:** 2026-07-14  
**Scope:** Post-land review of multi-target HTML outputs & cache namespaces  
**Files reviewed:** `src/target.zig`, `src/cache.zig`, `src/compile.zig`, `src/watch.zig`, `src/cli.zig`, `src/main.zig`, `docs/contracts/multi-target-isolated-output.md`

---

## Verdict

Architecture matches the first-slice contract (validate-all-first, one discovery, sequential sorted targets, structural cache isolation under each output root). Correctness gaps remain in path-boundary checks, usage exit codes, content-root collision, and cache manifest version hygiene. Docs/CHANGELOG drifted from the shipped CLI (`--target`, shared layout).

---

## Findings (severity)

| Sev | Finding | Disposition |
|-----|---------|-------------|
| **High** | Workspace containment used raw `startsWith` without path-boundary → sibling prefix false positives (`/ws` vs `/ws-evil`) | **Fix in this pass** |
| **High** | Target validation errors mapped to exit **3** (I/O) instead of **2** (usage) | **Fix in this pass** |
| **Medium** | No check that target roots overlap `--input` / layout roots (watch can ignore content; outputs pollute source tree) | **Fix in this pass** |
| **Medium** | On-disk cache `format_version` hardcoded `"boris-cache-v1"` while fingerprints use `"boris-cache-v1-multitarget"`; old manifests not explicitly rejected | **Fix in this pass** |
| **Medium** | CHANGELOG claimed `--html-target` / `--html-layout` / layout symlink checks; help omitted `--target` | **Fix in this pass** |
| **Low** | Per-event normalize of every target output dir in watch | Deferred (optional follow-up) |
| **Low** | Redundant per-target source re-read / dep-index rebuild / layout reload | Deferred (performance) |
| **Low** | No best-effort orphan atomic-temp scrub after kill | Deferred (hygiene) |
| **Info** | Per-page atomic publish only (not whole-tree staging) — matches contract wording | Document only |

### Already solid

- Validate-then-discover orchestration; shared PageDb promote once
- Structural cache under `<target-out>/.boris-cache/`
- Fingerprints include target name + layout path + layout bytes + format discriminator
- Watch ignores all configured target output roots; rebuilds serialized
- Per-file `createFileAtomic` + `replace` + temp cleanup on failure

---

## Work plan (this session)

### High — **done**

1. **Path-boundary helpers in `target.zig`**
   - Shared `hasAbsPathPrefix` / `pathsNestOrEqual` (case-aware for Windows/macOS)
   - Normalize separators in place; strip trailing `/`
   - Workspace membership + root collision + nest checks use boundary rules
   - Tests: boundary unit tests, nesting, siblings like `dist` vs `dist-prod`

2. **Exit code 2 for target validation**
   - `mapHtmlError` maps `DuplicateTargetName`, `InvalidTargetName`, `EmptyTargetDirectory`, `TargetOutputCollision`, `TargetOutputSymlink`, `WorkspaceEscape`, `NoTargetsSpecified` → `.usage`

### Medium — **done**

3. **Content / layout root non-overlap**
   - `validateTargets(..., ValidateTargetsOptions)` rejects target output that equals or nests with resolved content root, layout file path, and layout parent dir (when not workspace root)
   - Tests for `output_dir == content`, nested under content, and `layouts`

4. **Cache manifest version**
   - Write `cache.CACHE_FORMAT_VERSION` into manifest JSON
   - On load, ignore manifest if `format_version` ≠ current (cold rebuild)

5. **Docs alignment**
   - CHANGELOG corrected to `--target` / shared layout; review link
   - `cli.printUsage` documents `--target`
   - Contract: content/layout non-overlap + exit 2 + path-boundary wording
   - STATUS: Feature 5 **Done**; residual optional gaps listed

### Explicitly **not** in this pass

- Watch ignored-roots precompute
- Shared multi-target fingerprint/dep prep
- Orphan temp scrub
- Full component symlink walk
- Per-target layouts / selective watch fan-out

---

## Implementation notes

- Normative contract updated in the same change set.
- Keep single-threaded sequential multi-target scheduler.
- `zig build test` is the gate after edits.

---

## Follow-up candidates

| # | Item | Status |
|---|------|--------|
| 1 | Pre-normalize watch ignore roots once per coordinator lifetime | **Done** (`ignored_output_roots` on `WatchCoordinator`) |
| 2 | Shared include graph + source buffers across sequential targets | **Done** (`SharedCompileState` + `compilePagesWithShared`) |
| 3 | Best-effort atomic-temp scrub at target start | **Done** (`assemble.scrubStaleAtomicTemps`) |
| 4 | Intermediate path component symlink rejection | **Done** (`rejectSymlinkAlongPath`) |
| 5 | Help/`findBadArg` polish for `--target` | **Done** |

### Still optional (not required to close P3.3)

~~Per-target layouts / selective watch fan-out~~ **Done** (`--html-layout`, `--target-layout`, `selectTargetsForRebuild`)  
~~Whole-tree transactional publish~~ **Done** (sibling `{dist}.boris-stage` + rename commit; per-page atomic inside stage)

Residual: cross-volume stage rename not claimed; multi content-root per target still out of scope.
