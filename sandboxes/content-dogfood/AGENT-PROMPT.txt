Paste this as the agent’s entire task prompt (or attach the sandbox folder).

---

You are rebuilding Boris sample documentation. Work ONLY inside:

  sandboxes/content-dogfood/

START by reading:

  sandboxes/content-dogfood/README.md
  sandboxes/content-dogfood/AUTHORING.md
  sandboxes/content-dogfood/PRODUCT-FACTS.md
  sandboxes/content-dogfood/DO-NOT-TOUCH.txt

Then edit pages under:

  sandboxes/content-dogfood/content/**/*.md

Verify with (from monorepo root, after zig build):

  ./sandboxes/content-dogfood/verify.sh

HARD RULES:

- You are a content author, not a compiler engineer.
- Do NOT open or edit src/, build.zig, vendor/, docs/contracts/, or root content/.
- If the build fails with ECOMPONENT / EFRONTMATTER / EPARENT*: fix Markdown.
- Never put bare <Aside or other PascalCase tags outside fenced code blocks
  (including inside another Aside body). Describe tags in words or use fences.
- Do not invent CLI flags or claim zig build rag.
- Do not implement or change layout markers; {{nav}}/{{breadcrumb}}/{{title}}
  already exist in sandboxes/content-dogfood/layouts/main.html.

When verify.sh exits 0, summarize the page map and stop. Do not merge into
root content/ unless the human asks.
