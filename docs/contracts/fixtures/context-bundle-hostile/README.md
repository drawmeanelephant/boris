# Fixture: Context Bundle hostile export

**Contract target:** deterministic `boris-context` schema `1` output.

The fixture has an ordered semantic relation and a five-backtick source fence.
The focused hostile test verifies source-relative provenance, graph relation
preservation, dynamic fence sizing, repeat-export identity, failed-input
preservation, and staging cleanup.
