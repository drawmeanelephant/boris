# v0.1 overview — content compiler

**Status:** high-level orientation — **not** independently normative.  
Normative detail lives in the [canonical contracts](README.md). Living
implementation snapshot: [`docs/STATUS.md`](../STATUS.md).

---

## Product statement

Boris v0.1 is a **Zig content compiler** growing into an SSG: discover Markdown →
bounded frontmatter → Trunk/Satellite graph validation → **HTML site under
`dist/` by default** (Apex + Whiteboard), JSON IR under `.boris/` via `--out` /
`--no-rag`, and optional product RAG (`--rag` / `--rag-dir`). All modes share
the same graph rules. IR/RAG and pre-render coordination remain sequential;
HTML page render may use bounded `--jobs` workers.

Compiler id: `boris/0.1.1`. IR `schemaVersion`: `0.1.0`.

---

## Author-facing rules (summary)

| Topic | Rule |
|-------|------|
| Frontmatter | Closed grammar; not YAML — [frontmatter.md](frontmatter.md) |
| Keys | Exactly `id`, `title`, `parent`, `status`, `tags` |
| Parent key | **`parent` only** — never `parentEntry` |
| Trunk | No `parent` |
| Satellite | Exactly one `parent` naming a Trunk id |
| Paths / ids | `/` separators; no absolute, empty, `.`, or `..` segments — [identity-and-paths.md](identity-and-paths.md) |
| Extensions | Case-sensitive `.md` and `.mdx` only — [scanner.md](scanner.md) |
| Default output | HTML site under `dist/` — [html-output.md](html-output.md) |
| IR output | Deterministic JSON under `--out` (default `.boris`) — [ir-schema.md](ir-schema.md) |
| Asides | Constrained `<Aside>` on shared compile path; RAG may export `:::kind` (not authoring) — [components.md](components.md), [rag-export.md](rag-export.md) |
| HTML | Default CLI; also `--html` / `--html-dir` / `--target` — [html-output.md](html-output.md) |
| RAG | Optional product export — [rag-export.md](rag-export.md) |

---

## Implementation status (post-P2 / post-P3)

| Area | Status |
|------|--------|
| Contracts under `docs/contracts/` | **In force** (canonical list in [README.md](README.md)) |
| Root + contract fixtures | **Present**; IR goldens under `docs/contracts/fixtures/`; inventory under `fixtures/` |
| Default CLI | **Implemented** — bare `boris` is **HTML** (`dist/`); IR via `--out` / `--no-rag`; RAG opt-in |
| Scanner / identity / parser / graph / IR emit | **Implemented** |
| Aside tokenizer + `ECOMPONENT` | **Implemented** |
| Optional RAG + `:::kind` export | **Implemented** |
| Apex C ABI + Zig wrapper | **Implemented** (ApexMarkdown Unified adapter; not on IR/RAG *render* path) |
| HTML assemble | **Implemented** (default CLI + `--html` / `--html-dir` / `--target`) |
| P2 dependency indexes / fingerprints / `--incremental` | **Implemented** (HTML path) |
| P3.1 `--jobs` / P3.2 `--watch` / P3.3 multi-target | **Implemented** (HTML path) |
| HTML `dist/` as bare-`boris` default | **Implemented** (Feature 2) |
| Full YAML / MDX | **Out of scope** for v0.1 |

---

## Canonical contracts (do not use redirects)

| Document | Topic |
|----------|-------|
| [frontmatter.md](frontmatter.md) | Frontmatter grammar |
| [identity-and-paths.md](identity-and-paths.md) | Source paths and entity IDs |
| [scanner.md](scanner.md) | Discovery |
| [ir-schema.md](ir-schema.md) | Graph + JSON IR |
| [diagnostics.md](diagnostics.md) | Codes, severity, exits |
| [rag-export.md](rag-export.md) | Optional RAG |
| [components.md](components.md) | Aside tokenizer |
| [apex-abi.md](apex-abi.md) | Apex C ABI |
| [html-output.md](html-output.md) | Opt-in HTML |
| [parallel-rendering.md](parallel-rendering.md) | Bounded `--jobs` HTML workers |
| [watch-mode.md](watch-mode.md) | Opt-in `--watch` |
| [multi-target-isolated-output.md](multi-target-isolated-output.md) | Multi-target outputs |

Compatibility redirects only (non-normative): `parent-relationships.md`,
`source-path-and-id.md`, `json-ir-and-manifest.md` — see [README.md](README.md).

Acceptance checklist: [acceptance.md](acceptance.md).  
Living roadmap: [`docs/STATUS.md`](../STATUS.md).
