### Changed

- Removed obsolete frontmatter and historical harness modules; the active fuzz
  suite now exercises the authoritative `src/parser.zig` implementation. See
  the [frontmatter contract](/docs/contracts/frontmatter.md).
