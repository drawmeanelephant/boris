### Docs

- Replace stale `zig build -C tools/migration-lab` commands with the supported
  root-level form `zig build --build-file tools/migration-lab/build.zig` in the
  [migration-lab README](/tools/migration-lab/README.md), module comments, and
  WordPress fixture READMEs.
