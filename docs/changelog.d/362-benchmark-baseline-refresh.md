### Changed

- The benchmark regression gate baseline is refreshed for the phases fixed by
  the follow-on PERF cards: `fingerprint` and `graph_validate` limits are
  tightened to post-fix measurements (slower host value retained; all other
  phases unchanged) — see the
  [benchmark guide](/tools/testdata-generator/README.md).
