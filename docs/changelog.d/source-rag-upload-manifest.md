### Added

- Added `upload_manifest.json` for `boris-source-rag --bundles-only` exports with
  recommended upload order, on-disk sizes, total bytes, and a documented
  `chars/4` approximate token estimate; default exports stay unchanged. See the
  [source-RAG guide](/tools/source-rag/README.md).
