### Fixed

- The [editor](/content/guides/editor.md) **Delete file** dialog now renders a
  named placeholder when no file is selected: the confirmation reads
  "Delete selected file?" and the danger button reads "Delete file" instead of
  interpolating an empty target with trailing whitespace ([#461](https://github.com/drawmeanelephant/boris/issues/461)).