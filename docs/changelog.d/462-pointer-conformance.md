Tests: extend the interaction-hints conformance sweep with a pointer
block, asserting that clicking each dialog's primary and cancel buttons,
a command palette option, and the recovery banner actions produces the
same outcome the hinted key produces.
