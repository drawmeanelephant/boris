### Changed

- The GitHub Pages guide now states the `.nojekyll` policy explicitly:
  artifact-based `deploy-pages` uploads are served as-is and never need the
  marker, because Jekyll processing applies only to branch-based publishing.
  See [`docs/github-pages.md`](../github-pages.md).
