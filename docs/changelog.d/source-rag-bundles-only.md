### Added

- Added `boris-source-rag --bundles-only` to emit combined upload bundles and
  catalog sidecars without the per-file `files/**` tree; a successful rerun
  removes a prior `files/` pack via staged publication. See the
  [source-RAG guide](/tools/source-rag/README.md).
