### Docs

- Completed the cache-discriminator rename in the contracts: the multi-target
  isolation contract and the layout-rules fixture README now name
  `boris-cache-v3-nav-digest` as the current cache format instead of the
  retired `boris-cache-v2-layout-rules`, matching [cache.zig](/src/cache.zig)
  and the probe fixed in [#746](https://github.com/drawmeanelephant/boris/issues/746).
  See [multi-target-isolated-output](/docs/contracts/multi-target-isolated-output.md).
