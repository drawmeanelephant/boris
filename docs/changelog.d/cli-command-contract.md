---
title: "Stabilize the top-level CLI command contract"
category: "Changed"
---

Document the stable `build`, `check`, `impact`, and `watch` command surface and
accept explicit `build` and `watch` spellings while preserving the existing
watch flag compatibility path. Version the documentation-intelligence report
with graph nodes, typed edges, source locations, and diagnostics. See
[`docs/contracts/cli.md`](../contracts/cli.md) and the
[`documentation-intelligence contract`](../contracts/documentation-intelligence.md).
