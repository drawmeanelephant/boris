---
title: "Stabilize the top-level CLI command contract"
category: "Changed"
---

Document the stable `build`, `check`, `impact`, and `watch` command surface and
accept explicit `build` and `watch` spellings while preserving the existing
watch flag compatibility path. See [`docs/contracts/cli.md`](../contracts/cli.md).
