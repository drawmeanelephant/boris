<!--
Filename: 317-rag-working-context.md
Keep exactly one category heading. Replace this example link with a relevant
repository-root-relative link; contract-visible work links its updated contract.
-->

### Changed

- Product RAG now defaults to bounded, site-only working-context packs that
  preserve Boris-authorable Markdown and keep integrity metadata in a
  non-upload sidecar; use `--rag --complete` for the explicit full-corpus
  export. See the [RAG export contract](/docs/contracts/rag-export.md).
