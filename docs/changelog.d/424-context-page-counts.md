### Fixed

- Scoped context exports no longer print two ambiguous page counts: the
  bundle log reports the selected pages written under `pages/` and the
  CLI wrapper reports the full validated graph, with explicit
  "selected"/"graph" labels.
