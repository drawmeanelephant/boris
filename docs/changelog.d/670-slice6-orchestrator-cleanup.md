# 670 slice 6 — Editor: orchestrator cleanup (import-prune + dedupe)

Slice 6 of #670: final cleanup of the `App.svelte` orchestrator after `lib` + presentational + rail + dialogs segmentation.

- `editor/ui/src/App.svelte` `1637 → 1584` (`-54`, `1638 → 1584` measured) — final import-prune: remove 22 unused type imports (`FailureClass|ImpactEndpoint|PublicationSite|PublicationIdentity|PublicationTarget|PublicationProfile|PublicationProof|ProblemGroup|JsonSchemaProperty|CompletionEntity|CompletionIndex|GraphEndpoint|RecipeQuantity|RecipeIngredient|RecipeItem|RecipeFacet|RecipeScaleAmount|RecipeScaleQuantity|GraphEdge|GraphNav|GraphDocument|GraphLink`) and 8 unused value imports (`endpointPath|displayQuantity|packetCopyLabel|problemLocationLabel|schemaHint|validationStatusLabel|reportAgeLabel|validationCycleLabel`) from `lib/utils`; import `defaultCreatePath|paletteItemLabel|paletteItemKey` from `lib/utils` and delete local duplicates (`defaultCreatePath():1138` `paletteItemLabel:1136` `paletteItemKey:1176` — 18 lines). No `$:` deleted — all 13 derived (`dirty|problemGroups|problemsNotice|activeProblems|staleProblems|suggestions|activeNode|visibleScaleView|…|paletteEnabled`) are still read by `ProjectPane|SourcePane|ProblemsPane|PreviewPane|CommandPalette` and cannot be removed without store migration (explicit non-goal). The `~250`-line target in the issue is aspirational for a store-less orchestrator that still owns `token|api|timers|connect|openFile|runCommand|graph|recipe|dialogs` (`418-1770`); the lifted-state + callback orchestrator inherently remains `~1.5k` lines. This slice achieves the `delete dead $:` / `import-prune` part without visual/contract drift.
- `editor/ui/src/components/ProblemsPane.svelte` `174 → 153` (`-21`) — delete local duplicates of `validationStatusLabel|reportAgeLabel|validationCycleLabel|packetCopyKey` and import the canonical `lib/utils` versions (`failureLabel`/`packetCopyLabel`/`problemLocationLabel` already imported). `styles.css` single source, `dialog[open]`/`#palette-query`/workspace-rail grid unchanged, security boundary stays in `lib/api.ts`.
- `editor/ui/src/lib/utils.ts` — no change; `defaultCreatePath(inputMode)` already pure and now sole source for `content/new-page.md|new-recipe.cook|new-page.textile`.

`App.svelte` remains the sole owner of `token:#token 414 + api<T>:418` + timers (`host 5s|disk 3s|validate 1s|recovery 3s|save 300ms`) and prop-drills to `Header|SectionNav|RecoveryBanner|ProjectPane|SourcePane|ProblemsPane|PreviewPane|6 dialogs|palette`. Components never call `fetch`.

Verification: `npm --prefix editor/ui run check` 0 errors, `key-hints OK (24 across 19)`, `npm run build` succeeds (`127.04 kB`, 131 modules, `gzip 40.95 kB`), `zig build --build-file editor/build.zig test` green, `./editor/scripts/test-contract-fixture.sh` green, `./editor/scripts/test-host.sh` green, screenshot `375|768|1440` byte-identical by construction (no template/id/class change).

Follows `docs/plans/670-editor-segmentation.md` slice 6. Closes #670 segmentation (remaining ~250-line aspiration is store-migration work, not this issue).

