### Added

- Added bounded open semantic relation kinds, deterministic `{{relations}}` /
  `{{backlinks}}` HTML slots, and page-local incremental fingerprints for
  backlink rendering. See [the semantic-relations contract](../contracts/semantic-relations.md).
