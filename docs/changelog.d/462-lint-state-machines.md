Checks: extend the key-hint conformance lint to surface state machines —
the completion combobox's filter input must carry onfocus/oninput wiring
that reopens the list after Esc closes it, and the recovery banner's
Restore button must route a dirty buffer through the Save/Discard
resolution dialog.
