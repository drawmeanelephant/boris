### Added

- Promoted the original-voice example themes into [`themes/`](/themes/README.md)
  (`field-notes`, `compact`, `cards`, `cozy`, `journal`, `ledger`) and added
  [`reading`](/themes/reading/) for `:lang` typesetting. Sample sites remain
  under [`examples/`](/examples/README.md).
