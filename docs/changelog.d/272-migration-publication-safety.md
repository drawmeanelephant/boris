### Security

- Hardened WordPress and theme migration-lab output publication with owned
  sibling staging, source/output containment checks, and refusal of unowned
  output trees; repaired the report-first WordPress prototype so it is a valid
  Boris layout smoke input without claiming unmaterialised assets or mappings;
  added static-only FSE evidence inventory for `theme.json` and
  `templates/*.html` without block parsing or rendering.
  See the [migration-lab safety rules](/tools/migration-lab/README.md#safety-rules).
