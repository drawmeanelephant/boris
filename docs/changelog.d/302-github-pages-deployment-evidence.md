### Added

- Add a standalone, bounded Zig GitHub Pages post-deploy observer with a
  versioned deployment-evidence contract and separate retained workflow
  artifact; the optional audit binds observations to the normalized plan and
  exact target-local inventory without changing the public Pages payload. See
  [`github-pages-deployment-evidence`](../contracts/github-pages-deployment-evidence.md)
  and [`GitHub Pages workflow guidance`](../github-pages.md).
