### Docs

- Corrected the search guide's layout marker names to the normative
  `data-boris-search-root` / `data-boris-search-exclude`, and documented that an
  ordinary `boris` build publishes the search index itself rather than requiring
  a separate standalone run. Links:
  [the rendered-search contract](/docs/contracts/rendered-search.md).
