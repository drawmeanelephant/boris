### Added

- Added a focused process-level contract smoke covering explicit command
  routing, deterministic JSON goldens, repeated output, and distinct content,
  usage, and I/O exit classes. See [the CLI smoke](/test/cli-contract.sh) and
  [CLI contract](/docs/contracts/cli.md).
