Tests: add a keyboard-hints conformance sweep that walks every dialog, the
command palette, and the completion combobox, asserting each visible key
hint (Enter, Esc, Alt+S, Alt+D, arrow keys) still performs its action.
