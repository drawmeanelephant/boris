### Changed

- Standard.site is now a first-tester surface: `boris standard-site` prints
  the family list instead of "unexpected argument", `smoke` accepts
  `--handle`, and the operator path lives at
  [docs/standard-site.md](/docs/standard-site.md). Browser OAuth is still
  documented as failing closed on bsky.social; the working live path is
  `login --app-password`. See the
  [CLI contract](/docs/contracts/cli.md) and the
  [live smoke contract](/docs/contracts/atproto-live-smoke.md).
