### Added

- Added a top-level [authoring spine](/docs/authoring-spine.md): a
  non-normative teaching path — start → content & frontmatter → links &
  graph → layout → publish → verify — grounded in the `boris init` starter
  tree and linking into the contract warehouse instead of duplicating it.
  Each step reuses the closed vocabulary already published for editors
  (`boris-frontmatter-1.schema.json`, the `completion.json` entity/parent/
  relation/layout-slot vocabulary). Linked from the README.
