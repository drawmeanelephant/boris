### Changed

- Build the IR reverse dependency index in one edge pass while preserving the
  sorted, deduplicated [`graph.json` reverseIndex](/docs/contracts/ir-schema.md#reverse-dependency-index) bytes.
