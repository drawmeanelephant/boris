### Added

- Added the deterministic, stdout-only `boris plan --profile PATH` declaration
  surface for normalized publication intent, with a versioned [publication-plan
  contract](/docs/contracts/publication-plan.md) and schema.
