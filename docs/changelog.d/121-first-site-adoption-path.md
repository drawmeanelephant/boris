### Docs

- Document a single first real site adoption path in
  [docs/MIGRATION.md](/docs/MIGRATION.md): inspect → convert a bounded
  slice → review reports → build HTML / page-local assets / IR / RAG →
  exercise Aside, Details, includes, wiki and heading links, and layout
  rules → incremental edit → prod vs preview → deployable static output
  dir → analytics only as an explicit theme or host choice, with clear
  product vs migration-lab vs human-review boundaries.
