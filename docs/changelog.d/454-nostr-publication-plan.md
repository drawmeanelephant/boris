### Added

- Added an offline, deterministic Nostr NIP-23 publication plan:
  `boris nostr plan --profile PATH` reports the eligible articles, their
  publication-safe Markdown, and the tags each long-form event would carry,
  holding no key and contacting no relay. See the
  [Nostr publication contract](/docs/contracts/nostr-publication.md).
