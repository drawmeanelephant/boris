### Fixed

- The Cozy Corner and Node Tracker example themes no longer use the
  stylesheet asset URL as the `href` of navigation-like links; those
  visual-only controls are now explicit `static-control` placeholders,
  matching the convention Node Tracker already used.
