### Fixed

- The prototype examples' manual-review notes now match the shipped
  layouts: the Corporate review documents its real compiled-search
  integration and static theme/settings buttons instead of claiming a
  zero-JS design, the Minimalist layout's search input and icon buttons
  carry accessible names and explicit static markers, and the orphaned
  copy-button CSS rules were removed from both themes.
