### Changed

- Documented the stable `build`, `check`, `impact`, and `watch` command surface;
  explicit `build` and `watch` spellings preserve the existing watch-flag
  compatibility path. The documentation-intelligence report is versioned with
  graph nodes, typed edges, source locations, and diagnostics. See the
  [CLI contract](/docs/contracts/cli.md) and
  [documentation-intelligence contract](/docs/contracts/documentation-intelligence.md).
