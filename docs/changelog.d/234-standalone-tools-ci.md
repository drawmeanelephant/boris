### Added

- Added conditional Ubuntu/macOS CI coverage for the standalone
  `tools/search-index` and `tools/docs-maintenance` test gates. See the
  [release-gate workflow policy](/docs/RELEASE-GATE.md).
