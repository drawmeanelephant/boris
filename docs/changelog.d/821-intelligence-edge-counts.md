### Added

- `boris check` and `boris impact` reports now carry `edgeCounts` in the
  summary — incoming and outgoing edge counts by edge kind, split by endpoint
  class — closing the documented-but-missing gap in
  [the documentation-intelligence contract](/docs/contracts/documentation-intelligence.md).
  Links: [the schema](/docs/contracts/schemas/documentation-intelligence-0.2.0.schema.json),
  [#821](https://github.com/drawmeanelephant/boris/issues/821).
