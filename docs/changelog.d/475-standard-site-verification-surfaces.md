### Added

- Added Standard.site verification surfaces: a compiler-owned `{{head}}` slot
  for deterministic document link tags, well-known publication-record emission
  (with a base-path limitation report instead of a decoy), and a deterministic
  post-commit verification report. See the
  [Standard.site target contract](/docs/contracts/standard-site.md) and the
  [diagnostics contract](/docs/contracts/diagnostics.md).
