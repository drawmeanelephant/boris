### Added

- HTML publish copies page-owned sibling `<stem>.assets/` files, rewrites safe
  relative Markdown image URLs, and keeps theme `assets/` separate; see
  [content-local assets](/docs/contracts/content-local-assets.md).
