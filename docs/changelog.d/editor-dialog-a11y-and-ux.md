### Fixed

- Editor dialogs keep shortcut chips visible without gluing them into accessible names (`CancelEsc`), completion suggestions name value and detail separately, Create/Rename failures stay inside the dialog as human sentences, Graph is reachable with no file open, conflict compare panes reset to the top, and `/api/version` emits schema versions as `[1]` instead of a raw `0x01` byte. Links: [the editor-host contract](/docs/contracts/editor-host.md), [the editor guide](/content/guides/editor.md).
