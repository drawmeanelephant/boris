### Docs

- The retained Apex-era changelog fragments (vendored ApexMarkdown pin and
  its IR-schema build dependency) are disposed of as superseded by the
  Oliver renderer, and the v0.1 orientation record now marks its links to
  the removed Apex pipeline as archival by intent.
