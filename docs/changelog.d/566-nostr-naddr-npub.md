### Added

- `boris nostr plan` emits NIP-19 `author.npub`, per-article `naddr`, and
  `naddr_uri`. Profile `nostr.pubkey` accepts `npub1…` as well as hex.
  See the [Nostr publication contract](/docs/contracts/nostr-publication.md)
  and [#566](https://github.com/drawmeanelephant/boris/issues/566).
