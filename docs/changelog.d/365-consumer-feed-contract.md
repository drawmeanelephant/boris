### Docs

- Documented the external-consumer feed convention: `boris --out` IR plus the
  `{{content}}`-only HTML-mode body feed, output-relative hrefs, content-local
  assets, and deterministic rebuilds — normative description of the existing
  output surface, validated by two independent consumers (SvelteKit and a
  zero-dependency vanilla renderer)
  ([consumer feed contract](/docs/contracts/consumer-feed.md)).
