# Fixture inputs

The `themes/` directory contains small regular-file themes used to exercise
the `--theme` copy path. The supplied `large-markdown-corpus-benchmark.zip` is
kept here as design-study material; it is not unpacked by the generator and is
not included in generated fixture inventories.
