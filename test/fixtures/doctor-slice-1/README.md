# Doctor Slice 1 fixtures

Focused, read-only rendered-output inputs for the internal Doctor kernel.

- `site/` is a bounded two-page snapshot with nested, root-relative, query, and
  fragment links plus deployment-owned extra HTML.
- `hostile/` pins each malformed structure that makes rendered coverage
  incomplete.

The fixture is not a public Doctor report or publication-profile contract.
