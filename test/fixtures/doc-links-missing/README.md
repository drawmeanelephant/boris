# Missing published-link fixture

The single page contains missing relative and root-relative .md, .mdx, and
.html destinations, including nested paths. The HTML publication link audit
must report every local route as EROUTEMISSING, leave no committed target
tree, and return exit 1.
